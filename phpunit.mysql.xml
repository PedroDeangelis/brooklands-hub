<?xml version="1.0" encoding="UTF-8"?>
<!-- MySQL variant of phpunit.xml. SQLite is permissive about types MySQL is strict
     about (decimals come back as strings, for one), so run this before calling a
     piece of work done:
       lando ssh -s appserver -c "vendor/bin/phpunit -c phpunit.mysql.xml"
     Needs the database once: lando mysql -e "CREATE DATABASE IF NOT EXISTS laravel_test" -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="laravel_test"/>
        <env name="DB_HOST" value="database"/>
        <env name="DB_USERNAME" value="root"/>
        <env name="DB_PASSWORD" value=""/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- No real destination in tests: a delivery must be faked explicitly,
             never sent to whatever the developer's .env happens to point at. -->
        <env name="WEBSITE_SYNC_URL" value=""/>
        <env name="WEBSITE_SYNC_SECRET" value="testing-secret"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
