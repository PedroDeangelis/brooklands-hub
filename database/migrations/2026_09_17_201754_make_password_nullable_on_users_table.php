<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * Users sign in through Google, so most accounts never have a password.
     *
     * Written with the schema builder rather than a raw "ALTER TABLE ... MODIFY":
     * that syntax is MySQL's alone, and the test suite runs on SQLite, where it
     * is a syntax error that fails every migration run.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->string('password')->nullable()->change();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->string('password')->nullable(false)->change();
        });
    }
};
